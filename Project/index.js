'use strict'

const express = require('express');
const router = express.Router();
const app = express();

global.app = app;
global.router = router;

// starting the server
require()

module.exports = app;
module.exports = router;